# Intent Hub

一个基于向量相似度的静态路由系统，通过语义匹配将用户请求精准分发至对应的 AI Agent。

## ✨ 特性 / 贡献点

- 语义意图识别：摒弃传统的关键词匹配，采用 Qwen-Embedding-0.6B 高性能模型进行文本向量化，实现深层语义层面的路由分发。

- 高效向量检索：集成 Qdrant 向量数据库，支持大规模 Agent 意图的高效索引与毫秒级检索。

- 智能语料生成：利用 LLM 实现数据增强，为每个 Agent 自动生成多样化的测试语料（Utterances），大幅降低冷启动成本。

- 全栈管理界面：提供基于 Vue 3 + Element Plus 的直观管理后台，支持路由 CRUD、向量同步及系统配置。

- 灵活的 LLM 集成：通过 LangChain 统一接口，支持 DeepSeek、OpenRouter、通义千问、Gemini 等多种模型供应商。

- 自动化同步：具备热加载机制，支持本地 JSON 配置与 Qdrant 数据库的自动增量/全量同步。

## 📦 安装

### 后端环境 (Python 3.9+)

```shell
cd intent-hub-backend

pip install -r requirements.txt
```

### 前端环境 (Node.js 18+)

```shell
cd intent-hub-frontend

npm install
```

## 🚀 快速开始

1. 准备向量数据库：

确保 Qdrant 实例已运行（推荐使用 Docker）：

```shell
docker run -p 6333:6333 qdrant/qdrant
```

2. 配置后端：

在 intent-hub-backend/intent_hub 目录下，将 settings.example.json 复制并重命名为 settings.json，填写您的 API Key 及 Qdrant 地址。

3. 启动后端服务

```shell
cd intent-hub-backend

python run.py
```

4. 启动前端界面

```shell
cd intent-hub-frontend

npm run dev
```

访问控制台输出的地址（通常为 http://localhost:5173）。

## 📘 使用方法

1. 创建路由：在管理后台添加一个新的 Agent 路由，定义其名称、描述及核心功能。
2. 生成语料：点击“生成例句”，系统将调用 LLM 根据描述自动生成多样化的用户提问。
3. 向量同步：保存路由后，系统会自动将其向量化并存储至 Qdrant。
4. 预测接口：通过 API 调用 /predict 接口，传入用户文本，获取匹配度最高的 Agent ID。

## 📂 项目结构

intenthub/

├── intent-hub-backend/    # Python 后端

│  ├── intent_hub/      # 核心逻辑 (编码、检索、服务层)

│  ├── docs/         # 项目文档 (API, PRD, 结构说明)

│  ├── tests/        # 单元测试

│  └── run.py        # 后端入口

└── intent-hub-frontend/   # Vue 3 前端

  ├── src/         # 源代码 (页面、组件、状态管理)

  ├── public/        # 静态资源

  └── vite.config.ts    # Vite 配置

## 🔧 配置说明

后端核心配置文件为 settings.json，主要参数包括：

- QDRANT_URL: 向量数据库连接地址。

- EMBEDDING_MODEL_NAME: 嵌入模型路径（支持 HuggingFace 或本地路径）。

- LLM_PROVIDER: 语料生成的 LLM 供应商。

- AUTH_ENABLED: 是否开启 API 认证。

## 🐳 Docker 使用

项目根目录下提供了 Dockerfile（位于 backend），支持容器化部署：

```shell
docker build -t intent-hub-backend ./intent-hub-backend

docker run -p 5000:5000 intent-hub-backend
```

## 🤝 贡献指南

1. Fork 本项目。
2. 创建您的特性分支 (git checkout -b feature/AmazingFeature)。
3. 提交您的更改 (git commit -m 'Add some AmazingFeature')。
4. 推送到分支 (git push origin feature/AmazingFeature)。
5. 开启一个 Pull Request。

## 📄 License

Distributed under the MIT License. See LICENSE for more information.